export const MockStorageService = {
  set(_key: string, _data: any, _isSession = false) {
    // Set session storage data
  },

  get(_key: string, _isSession = false) {
    // Get storage data
  },

  remove(_key: string, _isSession = false) {
    // Remove data
  },

  clear(_isSession = false) {
    // Clear Storage
  },
};
